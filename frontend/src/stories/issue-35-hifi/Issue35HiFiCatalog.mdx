import { Canvas, Description, Meta, Subtitle, Title } from '@storybook/addon-docs/blocks';
import * as LayoutStories from './Issue35ParticipantLayoutHiFi.stories';
import * as TopBarStories from './Issue35TopBarMock.stories';
import * as SessionCardStories from './Issue35SessionCardMock.stories';
import * as ReservationStories from './Issue35ReservationPanelMock.stories';
import * as MyPageStories from './Issue35MyPageReadablePanelMock.stories';

<Meta of={LayoutStories} />

<Title>Issue #35 予約情報可読化 HiFi カタログ</Title>
<Subtitle>US-17: タイトル・時刻・トラック中心表示への移行</Subtitle>

<Description>
  目的は、マイページ予約表示の `sessionId` 依存を廃止し、
  参加者が当日の行動判断をしやすい表示順序を実装前に合意すること。
</Description>

## 1. 画面レイアウトストーリー

<Canvas of={LayoutStories.Default} />
<Canvas of={LayoutStories.LoadingKeepList} />
<Canvas of={LayoutStories.ErrorKeepList} />
<Canvas of={LayoutStories.Success} />
<Canvas of={LayoutStories.MixedFallbackText} />
<Canvas of={LayoutStories.EmptyReservations} />
<Canvas of={LayoutStories.ManyReservations} />

## 2. 実装コンポーネントストーリー

<Canvas of={TopBarStories.Mobile} />
<Canvas of={SessionCardStories.Primary} />
<Canvas of={ReservationStories.WithCancelCta} />
<Canvas of={MyPageStories.Default} />
<Canvas of={MyPageStories.LoadingKeepList} />
<Canvas of={MyPageStories.ErrorKeepList} />
<Canvas of={MyPageStories.ErrorInitialEmpty} />
<Canvas of={MyPageStories.MixedFallbackRow} />
<Canvas of={MyPageStories.Empty} />
<Canvas of={MyPageStories.ManyReservations} />

## 3. 想定デバイス

- Primary: 参加者スマートフォン（390x844）
- Secondary: 受付補助タブレット（834x1112）
- Tertiary: 来場前PC確認（1280幅）

## 4. 受け入れ条件との対応

- CP1（sessionId依存表示改善）: `MyPageReadablePanelMock` の `MixedFallbackRow` / `AllFallback` で固定
- CP2（可読性向上）: タイトル主表示 + `HH:mm | Track X` 副表示を全ストーリーに反映
- CP3（キャンセル導線維持）: `ReservationPanelMock` で `キャンセル` CTA を維持

## 5. 境界ケース

- 0件: `Empty` / `EmptyReservations`
- 多件: `ManyReservations`
